version: '2.4'
services:
  db:
    image: mysql
    container_name: ${DB_NAME}
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_HOST: '%'
    ports:
      - "${DB_PORT}:3306"
    volumes:
      - java_training_data:/var/lib/mysql

volumes:
  java_training_data: